import { component$ } from "@builder.io/qwik";

export const Field = component$(() => {
    const style = {
        height: "100vh",
        backgroundColor: "black",
        color: "white"
    };

    return (
        <div style={style}>
            a
        </div>
    )
})